console.clear();
const prompt = require("prompt-sync")();

let base = Number(prompt("base: "));

let altura = Number(prompt("altura: "));

if (true) {
    console.log(`perimetro: ${2*(base+altura)} y superficie: ${base*altura}`)
    
}